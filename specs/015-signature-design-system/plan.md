# Implementation plan

1. Preserve the dirty worktree on `feat-kya-signature-integration`.
2. Extract approved shared tokens and assets; replace the obsolete shell stylesheet.
3. Rebuild the React shell and home composition around existing destinations.
4. Complete the portable skill and expose its real files in the local Studio.
5. Run type checks, unit tests, production build, skill validation and desktop/mobile
   browser journeys. Record limitations and evidence in tasks.md.
