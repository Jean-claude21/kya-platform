# Plan — Real AI environments v0.1

1. Add a read-only broker query for the user's latest active connectors.
2. Expose the sanitized connector list through the governed MCP profile API.
3. Replace static frontend clients with the discovered list and resilient UI states.
4. Add contract and component tests, then run backend and web quality gates.
