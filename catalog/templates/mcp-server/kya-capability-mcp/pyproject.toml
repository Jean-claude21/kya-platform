[project]
name = "kya-capability-mcp"
version = "0.1.0"
requires-python = ">=3.14,<3.15"
dependencies = ["mcp>=2,<3", "pydantic>=2.12"]

[dependency-groups]
dev = ["pytest>=9", "ruff>=0.13", "mypy>=1.18"]
